{
  "name": "accelerate_runner",
  "scripts": {
    "record": "./record_and_upload.sh --no-video",
    "start": "python lib/send_command_to_server.py",
    "test": "python -m pytest -q test/solution_tests/"
  }
}
